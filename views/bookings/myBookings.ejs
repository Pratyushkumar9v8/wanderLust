<% layout("/layouts/boilerplate") %>

<h2>My Bookings</h2>
<ul>
  <% bookings.forEach(booking => { %>
    <li>
      <strong><%= booking.listing.title %></strong> <br>
      From: <%= booking.startDate.toDateString() %> To: <%= booking.endDate.toDateString() %>
    </li>
  <% }) %>
</ul>
