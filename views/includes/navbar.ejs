<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" ><span style="font-family: 'Poppins', sans-serif;  font-weight: 600;">
      <i class="fa-solid fa-compass"></i> &nbsp;Wanderlust
    </span>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse bg-white " id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mt-2 mb-2 mb-lg-0 ">
        <li class="nav-item">
          <a class="nav-link" href="/listings">Explore</a>
        </li>
        <% if(currentUser){ %> 
        <li class="nav-item">
          <a class="nav-link" href="/wishlist">Wishlists</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="//bookings/userBookings">Bookings</a>
        </li>
        <% } %>
        
      </ul>
      <ul class="navbar-nav ml-auto mt-2 mb-2 mb-lg-0 ">
        
        <% if(currentUser){ %> 
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                Account
              </a>
              <ul class="dropdown-menu">
                <li >
                        <a class="dropdown-item" >
                        <div class="user-logo">
                          <p style="margin-bottom: 5px;"><%= currentUser.username %></p>
                        <% if (currentUser.profilePicture) { %>
                      <img 
                        valu
                        src="<%= currentUser.profilePicture %>" 
                        alt="User profile" 
                        title="<%= currentUser.email %>" 
                        class="rounded-circle mb-2" 
                        style="width: 35px; height: 35px; object-fit: cover; border: 2px solid #ddd;"
                      >
                    <% } else { %>
                      <div title="<%= currentUser.email %>" 
                        class="rounded-circle bg-secondary text-white d-flex align-items-center justify-content-center mb-2"
                        style="width: 35px; height: 35px; font-size: 32px; font-weight: bold;"
                      >
                        <%= currentUser.username.charAt(0).toUpperCase() %>
                      </div>
                    <% } %>
                        </div>
                    </a> 
                </li>
                <li><a class="dropdown-item" title="<%= currentUser.email %>"><%= currentUser.email %></a></li>
                <li><hr class="dropdown-divider"></li>
                <li class="dropdown-item" ><a class="nav-link"  href="/user/logout">Logout</a></li>  

              </ul>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/listings/new">Become a host </a>
            </li>
            <%}%>

          <% if(!currentUser){ %> 
                <li class="nav-item"><a class="nav-link" href="/user/login">Login</a></li>
                <li class="nav-item"><a class="nav-link" href="/user/signup">SignUp</a></li>
           <% }%>
          
        
      </ul>
    </div>
  </div>
</nav>